hidden subset
pair
	各行列ブロックでマスクしたものをカウント
	カウントが2のものを選ぶ
	2のものを比較して等しければhidden pairが検出できる
		2のものが同じ行列ブロックに属している
	その行列ブロックにそのpairをマスクする
triple
	カウントが3のものを選んで比較する
subset
	カウントが2のものを登録

naked subset
pair
	16ビット、256個の配列に変換
	2つだけビットが立っている部分を列挙
	同じ列、行、ブロックならばnaked pairが検出できている
	その列、行、ブロックの見つかった数字のペアを消す

x-wing
	ある行のビット数が2で、別の行に同じビットパターンがある

generate()
	適当にNマス埋める
	繰り返す
		vectorのfrontを消す
		解けるところまで解く
		候補を展開して、それぞれを入れてスコア算出
		スコアが最も良いものを選び、push_backする

set_random() 適当に1マス埋める
	reconstructする
	update
		エラーはそのまま例外を投げる
	空きマスを求める
	ランダムに空きマスを選ぶ
	ランダムに入れられる数字を入れる

試している時
	pop
	reconstruct
	update
	set
	update

確定するとき
	push
	reconstruct
	update
